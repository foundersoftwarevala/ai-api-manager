  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

@  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

8  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

G  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

<  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

<  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

<  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

!  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

^  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

\  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

\  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

H  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

5  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

!  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

7  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

+  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

3  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

G  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

5  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

9  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

9  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

@  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

L  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

G  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

<  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

R  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

!  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

K  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

L  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

V  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

B  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

L  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

K  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

Y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

!  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

K  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

3  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

|  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

5  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

3  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

z  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

B  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

$  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

K  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

T  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

j  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

J  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

Y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

P  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

-  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

!  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

4  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

9  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

4  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

E  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

$  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

$  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

j  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

<  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

>  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

h  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

[  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

]  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

+  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

/  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

*  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

?  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

=  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

D  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

S  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

4  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

4  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

+  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

F  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

6  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

f  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

g  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

w  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

A  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

C  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

q  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

1  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

F  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

6  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

_  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

2  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

0  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

"  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

$  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

$  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

{  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

v  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

`  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

p  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

I  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

k  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

O  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

:  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

N  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

u  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

m  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

b  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

r  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

U  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

.  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

o  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

F  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

i  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

x  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

d  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

(  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

6  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

)  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

l  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

a  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

t  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

e  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

n  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

c  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

y  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

M  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

s  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

,  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

   // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

;  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

}  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";


  // The gateway serves a fixed catalogue of chat models; catalogue rows that
  // are not chat-servable (speech, image, embeddings, non-gateway vendors)
  // fall back to a served text model so the probe still exercises a real call.
  const requested = String(model.model_id);
  const chatServable =
    /^(openai\/gpt-5|google\/gemini-[23])/.test(requested) &&
    !/(image|tts|transcribe|embedding)/.test(requested);
  const served = chatServable ? requested : "google/gemini-3.5-flash";

